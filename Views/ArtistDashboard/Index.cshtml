@model List<UBB_SE_2024_Music.DTO.SongBasicInformation>

<h1>Artist Songs</h1>

<ul>
    @foreach (var song in Model)
    {
        <li>
            @Html.ActionLink(song.Name, "Details", "ArtistDashboard", new { songId = song.SongId }, null)
        </li>
    }
</ul>
